import TabsSettings from "./TabsSettings";
import UnitsSettings from "./UnitsSettings";

const Settings = () => {
  return (
    <>
      <UnitsSettings />
      <TabsSettings />
    </>
  );
};

export default Settings;
